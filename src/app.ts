import { SessionState } from './types.ts';

// eslint-disable-next-line @typescript-eslint/no-unused-vars -- stubbed future engine; `args` is the intended signature (see LLM_INSTRUCTIONS.md).
export function startSession(args: SessionState): void {
  throw new Error('TODO: implement.');
}
